import type { CaptureItem } from './types';
export interface Project { id:string; title:string; date:string; items:CaptureItem[] }
async function db() { return new Promise<IDBDatabase>((resolve,reject)=>{const req=indexedDB.open('snapdeck',1);req.onupgradeneeded=()=>req.result.createObjectStore('projects',{keyPath:'id'});req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error);}); }
export async function saveProject(project:Project) { const database=await db();try {await new Promise<void>((resolve,reject)=>{const tx=database.transaction('projects','readwrite');tx.objectStore('projects').put(project);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error);});}finally{database.close();} }
export async function getProjects() {const database=await db();try{return await new Promise<Project[]>((resolve,reject)=>{const req=database.transaction('projects').objectStore('projects').getAll();req.onsuccess=()=>resolve(req.result.sort((a:Project,b:Project)=>b.date.localeCompare(a.date)));req.onerror=()=>reject(req.error);});}finally{database.close();}}
export async function deleteProject(id:string){const database=await db();try{await new Promise<void>((resolve,reject)=>{const tx=database.transaction('projects','readwrite');tx.objectStore('projects').delete(id);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error);});}finally{database.close();}}
